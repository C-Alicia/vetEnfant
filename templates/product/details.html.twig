{% extends 'layout/base.html.twig' %}

{% block title %}Détails {{ product.name }}{% endblock %}

{% block body %}

<section class="bg-light p-3 p-md-4 p-xl-5 mt-4">
    <article class="container container-lg pt-5"> {# Ajout de la classe pt-5 pour l'espace en haut #}
        <div class="row justify-content-center">
            <div class="col-12 col-xxl-11">
                <div class="card border-light-subtle shadow-sm">
                    <div class="row g-0 align-items-center">
                        <!-- Image à gauche -->
                        <!-- Image principale -->
                        <div class="col-12 col-md-6 m-4">
                            <img src="{{ asset('assets/uploads/products/' ~ product.image[0].src ) }}"
                                alt="{{ product.name }}"
                                class="img-fluid rounded-start object-fit-cover mb-3 product-image" id="mainImage">

                            <!-- Miniatures d'images -->
                            <div class="d-flex">
                                {% for image in product.image %}
                                <img src="{{ asset('assets/uploads/products/mini/300x300-' ~ image.src) }}"
                                    alt="{{ product.name }}"
                                    class="thumbnail rounded mb-3 {% if loop.first %}active{% endif %}"
                                    onclick="changeImage(event, '{{ asset('assets/uploads/products/mini/300x300-' ~ image.src) }}')"
                                    onkeypress="handleKeyPress(event)">
                                {% endfor %}
                            </div>
                        </div>

                        <!-- Formulaire à droite -->
                        <div class="col-12 col-md-6 m-4">
                            <div class="col-12 col-lg-11 col-xl-10">
                                <div class="card-body p-3 p-md-4 p-xl-5">
                                    <!-- Titre -->
                                    <h2 class="mb-3">{{ product.name }}</h2>
                                    <div class="text-muted mb-4 d-flex align-items-center">
                                        <a href="#" class="text-decoration-none text-muted">
                                            <!-- Default image fallback using asset() -->
                                            <img src="{{ product.user is not null and product.user.profileImage is not null ? product.user.profileImage : asset('images/avatarDefault.jpg') }}"
                                                alt="Photo du vendeur" class="rounded-circle me-2"
                                                style="width: 40px; height: 40px;">

                                            <!-- Default username fallback -->
                                            <span>{{ product.user is not null ? product.user.username : 'Unknown User'
                                                }}</span>
                                        </a>
                                    </div>

                                    <div class="mb-3">
                                        <span class="h4 me-2">Prix : {{ product.price }} €</span>
                                    </div>
                                    <p class="mb-4 text-justify">{{ product.description }}</p>
                                    <div class="mb-4">
                                        <p class="product-detail"><strong>Taille :</strong> {{ product.getAgeCategory()
                                            }}</p>
                                        <p class="product-detail"><strong>État :</strong> {{ product.state.value }}</p>
                                    </div>

                                    <!-- Boutons -->
                                    <div class="d-grid"> {# ajouter un lien vers la page favoris #}
                                        {{ include('components/button.html.twig', { class: 'btn-general', label:
                                        'Ajouter aux favoris', type: 'submit' }) }}
                                    </div>
                                    <div class="d-grid"> {# ajouter un lien vers la page du vendeur #}
                                  {{ include('components/button.html.twig', { class: 'btn-general', label:
                                        'Contacter le vendeur', type: 'submit' }) }}
                                    </div>
                                    
                                    {# ajouter un lien vers la page panier #}
                                    <div class="d-grid">                                
                                    {{ include('components/button_with_link.html.twig', {
                                        class: 'btn-general',
                                        href: path('cart_add', { id: product.id }),
                                        type: 'submit',
                                        label: 'Ajouter au panier'
                                    }) }}
                                </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </article>
</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script>
    function changeImage(event, src) {
        const mainImage = document.getElementById('mainImage');
        const thumbnails = document.querySelectorAll('.thumbnail');

        mainImage.src = src;
        thumbnails.forEach(thumb => thumb.classList.remove('active'));
        event.target.classList.add('active');
    }
</script>

{% endblock %}